apiVersion: batch/v1
kind: Job
metadata:
  name: pullup-test-e2e
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: pullup-test-e2e
          image: pullup-test-e2e
          imagePullPolicy: IfNotPresent
