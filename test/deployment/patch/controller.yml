apiVersion: apps/v1
kind: Deployment
metadata:
  name: pullup-controller
spec:
  template:
    spec:
      containers:
        - name: controller
          env:
            - name: LOG_LEVEL
              value: debug
