apiVersion: apps/v1
kind: Deployment
metadata:
  name: pullup
spec:
  template:
    spec:
      containers:
        - name: pullup
          imagePullPolicy: Never
          env:
            - name: LOG_LEVEL
              value: debug
